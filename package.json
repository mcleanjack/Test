{
  "name": "letterbox-turntable",
  "version": "1.0.0",
  "description": "Interactive 3D turntable viewer for the Integrated Letter Box model — a single self-contained HTML file (dist/artifact.html) with Three.js, materials, sketch-style edge outlines, and click-to-isolate parts.",
  "private": true,
  "type": "module",
  "scripts": {
    "bundle": "esbuild entry.js --bundle --format=iife --global-name=Bundle --minify --outfile=.build/bundle.min.js",
    "assemble": "node build.mjs",
    "build": "npm run bundle && npm run assemble"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  },
  "dependencies": {
    "three": "^0.160.0"
  }
}
