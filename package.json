{
  "name": "mayde-tate-house-viewer",
  "version": "1.0.0",
  "description": "Interactive 3D building-information viewer for the Mayde Tate house (Revit-derived GLB) — Three.js with sketch-style edge outlines, click-to-isolate elements, and 3D construction/product callouts anchored to the model's own Revit sphere markers.",
  "private": true,
  "type": "module",
  "scripts": {
    "bundle": "esbuild entry.js --bundle --format=iife --global-name=Bundle --minify --outfile=.build/bundle.min.js",
    "assemble": "node build.mjs",
    "build": "npm run bundle && npm run assemble"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  },
  "dependencies": {
    "three": "^0.160.0"
  }
}
